apply plugin: 'idea'

subprojects {
    apply plugin: 'idea'

    idea {
        module {
            sourceDirs += files('src')
        }
    }
}

idea {
    project {
        modules = modules.findAll({ it.name != name })
    }
}
